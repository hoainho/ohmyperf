var O={"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"};function n(e){return e==null?"":(typeof e=="string"?e:String(e)).replace(/[&<>"']/g,r=>O[r]??r)}function h(e){return JSON.stringify(e).replace(/</g,"\\u003c").replace(/>/g,"\\u003e").replace(/\u2028/g,"\\u2028").replace(/\u2029/g,"\\u2029")}var b=`
*, *::before, *::after { box-sizing: border-box; }
:root {
  color-scheme: light dark;
  --bg: #f7f7f8;
  --panel: #ffffff;
  --border: #e5e7eb;
  --muted: #6b7280;
  --text: #111827;
  --accent: #4338ca;
  --good: #15803d;
  --warn: #b45309;
  --bad: #b91c1c;
  --code-bg: #f3f4f6;
  --code-border: #e5e7eb;
  --table-stripe: #fafafa;
}
@media (prefers-color-scheme: dark) {
  :root {
    --bg: #0b1020;
    --panel: #111827;
    --border: #1f2937;
    --muted: #94a3b8;
    --text: #e5e7eb;
    --accent: #818cf8;
    --good: #4ade80;
    --warn: #fbbf24;
    --bad: #f87171;
    --code-bg: #0f172a;
    --code-border: #1f2937;
    --table-stripe: #0d1525;
  }
}
html, body { margin: 0; padding: 0; }
body {
  background: var(--bg);
  color: var(--text);
  font: 14px/1.45 -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
  padding: 24px;
}
.container { max-width: 1100px; margin: 0 auto; }
h1, h2, h3 { margin-top: 0; }
h1 { font-size: 22px; font-weight: 600; letter-spacing: -0.01em; }
h2 { font-size: 16px; font-weight: 600; margin: 28px 0 12px; letter-spacing: -0.005em; }
h3 { font-size: 13px; font-weight: 600; margin: 0 0 8px; color: var(--muted); text-transform: uppercase; letter-spacing: 0.05em; }
.muted { color: var(--muted); }
.mono { font-family: ui-monospace, SFMono-Regular, "SF Mono", Menlo, monospace; font-size: 12.5px; }
.panel {
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 16px 18px;
  margin-bottom: 16px;
}
.meta {
  display: grid;
  grid-template-columns: max-content 1fr;
  gap: 4px 16px;
}
.meta dt { color: var(--muted); }
.meta dd { margin: 0; word-break: break-all; }
.tiles {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  gap: 12px;
}
.tile {
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 14px 16px;
}
.tile .name { color: var(--muted); font-size: 12px; text-transform: uppercase; letter-spacing: 0.05em; }
.tile .value { font-size: 22px; font-weight: 600; margin-top: 4px; }
.tile .sub { color: var(--muted); font-size: 11.5px; margin-top: 2px; }
.tile.unstable { border-color: var(--warn); }
.tile.unstable .sub { color: var(--warn); }
.unstable-banner {
  background: color-mix(in srgb, var(--warn) 15%, var(--panel));
  border-left: 3px solid var(--warn);
  padding: 12px 14px;
  border-radius: 6px;
  margin-bottom: 16px;
}
table { width: 100%; border-collapse: collapse; }
th, td { padding: 8px 10px; text-align: left; border-bottom: 1px solid var(--border); }
th { font-weight: 600; color: var(--muted); font-size: 12px; text-transform: uppercase; letter-spacing: 0.04em; }
tbody tr:nth-child(even) { background: var(--table-stripe); }
.tag { display: inline-block; padding: 1px 8px; border-radius: 999px; font-size: 11px; font-weight: 600; }
.tag.pass { background: color-mix(in srgb, var(--good) 18%, transparent); color: var(--good); }
.tag.fail { background: color-mix(in srgb, var(--bad)  20%, transparent); color: var(--bad); }
.tag.warn { background: color-mix(in srgb, var(--warn) 20%, transparent); color: var(--warn); }
details { margin: 0; }
details > summary { cursor: pointer; font-weight: 600; padding: 6px 0; }
pre.code {
  background: var(--code-bg);
  border: 1px solid var(--code-border);
  border-radius: 8px;
  padding: 12px 14px;
  overflow-x: auto;
  white-space: pre;
  margin: 8px 0 0;
  font-family: ui-monospace, SFMono-Regular, "SF Mono", Menlo, monospace;
  font-size: 12px;
}
.frame-tree ul { list-style: none; padding-left: 18px; margin: 4px 0; }
.frame-tree li { margin: 4px 0; }
.frame-tree .frame-url { color: var(--muted); font-family: ui-monospace, monospace; font-size: 11.5px; }
.foot { color: var(--muted); font-size: 12px; margin-top: 24px; }
.foot a { color: var(--accent); }
`;var T=[{name:"lcp",unit:"ms",digits:1},{name:"fcp",unit:"ms",digits:1},{name:"ttfb",unit:"ms",digits:1},{name:"inp",unit:"ms",digits:1},{name:"cls",unit:"score",digits:3},{name:"tbt",unit:"ms",digits:1}],v=.2;function $(e,t={}){let r=t.title??`OhMyPerf \u2014 ${V(e.meta.url)}`,a=t.embedReportPayload!==!1;return`<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<meta name="generator" content="@ohmyperf/viewer 0.0.0-pre" />
<meta name="referrer" content="no-referrer" />
<title>${n(r)}</title>
<style>${b}</style>
</head>
<body>
<div class="container">
  ${B(e)}
  ${N(e)}
  ${C(e)}
  ${H(e.audits)}
  ${P(e)}
  ${j(e)}
  ${L(e)}
  ${D(e.pluginData)}
  ${J(e)}
  <p class="foot">
    Generated by <code>@ohmyperf/viewer</code>. This file is self-contained:
    no external network requests are made when you open it.
  </p>
</div>
${a?U(e):""}
</body>
</html>
`}function B(e){let t=e.meta;return`<div class="panel">
  <h1>OhMyPerf v${n(e.schemaVersion)} report</h1>
  <dl class="meta">
    <dt>URL</dt><dd class="mono">${n(t.url)}</dd>
    <dt>Started</dt><dd>${n(t.startedAt)}</dd>
    <dt>Duration</dt><dd>${n(`${String(t.durationMs)} ms`)}</dd>
    <dt>Mode</dt><dd>${n(t.mode)} \xB7 runs=${n(String(t.runs))} \xB7 ${n(t.parity.mode)}</dd>
    <dt>Browser</dt><dd>${n(`${t.browser.name} ${t.browser.version} (${t.browser.source})`)}</dd>
    <dt>Host</dt><dd>${n(`${t.host.os} (${t.host.arch}) \xB7 Node ${t.host.nodeVersion}`)}</dd>
    <dt>Measurement ID</dt><dd class="mono">${n(t.measurementId)}</dd>
  </dl>
</div>`}function N(e){return z(e)?`<div class="unstable-banner">
  <strong>Unstable run.</strong> At least one Core Web Vital has CoV &gt; ${n(String(v*100))}% across the ${n(String(e.runs.length))} run(s). Increase <code>--runs</code> or use <code>--mode ci-stable</code> for budget gating.
</div>`:""}function z(e){if(e.meta.unstable===!0)return!0;for(let t of["lcp","cls","inp","fcp","ttfb"]){let r=e.aggregated[t];if(r&&Number.isFinite(r.cov)&&r.cov>v)return!0}return!1}function C(e){let t=T.map(({name:r,unit:a,digits:o})=>{let d=e.aggregated[r];return A(r,d,a,o)}).filter(r=>r.length>0).join(`
`);return t.length===0?"":`<h2>Aggregated metrics</h2>
<div class="tiles">
${t}
</div>`}function A(e,t,r,a){if(!t)return"";let o=Number.isFinite(t.cov)&&t.cov>v,d=t.median.toFixed(a),c=r==="ms"?`${d} ms`:d,i=Number.isFinite(t.cov)?`${(t.cov*100).toFixed(1)}%`:"\u2014";return`  <div class="tile${o?" unstable":""}">
    <div class="name">${n(e.toUpperCase())}</div>
    <div class="value">${n(c)}</div>
    <div class="sub">cov ${n(i)} \xB7 n=${n(String(t.runs))}${o?" \xB7 unstable":""}</div>
  </div>`}function H(e){return e.length===0?"":`<h2>Audits</h2>
<div class="panel" style="padding:0;overflow:hidden;">
  <table>
    <thead><tr><th>Status</th><th>ID</th><th>Title</th><th>Score</th></tr></thead>
    <tbody>
${e.map(r=>{let a=r.passed?"pass":"fail",o=r.passed?"PASS":"FAIL";return`    <tr>
      <td><span class="tag ${n(a)}">${n(o)}</span></td>
      <td class="mono">${n(r.id)}</td>
      <td>${n(r.title)}</td>
      <td>${r.score===null?'<span class="muted">\u2014</span>':n(String(r.score))}</td>
    </tr>`}).join(`
`)}
    </tbody>
  </table>
</div>`}function P(e){let t=e.runs.find(s=>s.cold),a=e.runs.find(s=>!s.cold)??t??e.runs[0];if(!a||a.resources.length===0)return"";let o=[...a.resources];o.sort((s,l)=>s.requestMs+s.responseMs-(l.requestMs+l.responseMs));let d=o.reduce((s,l)=>s+(l.encodedSizeBytes||0),0),c=o.reduce((s,l)=>s+(l.transferSizeBytes||0),0),i=o.filter(s=>s.renderBlocking).length,u=`${String(o.length)} resources \xB7 ${x(d)} encoded \xB7 ${x(c)} transfer${i>0?` \xB7 <span class="tag warn">${String(i)} render-blocking</span>`:""}`,F=o.slice(0,100).map(s=>{let l=(s.requestMs+s.responseMs).toFixed(1),E=s.dnsMs!==void 0?s.dnsMs.toFixed(1):"\u2014",k=s.tcpMs!==void 0?s.tcpMs.toFixed(1):"\u2014",I=s.tlsMs!==void 0?s.tlsMs.toFixed(1):"\u2014",g=[];return s.renderBlocking&&g.push('<span class="tag warn">render-block</span>'),s.cacheHit&&g.push('<span class="tag pass">cache</span>'),`    <tr>
      <td class="mono" style="max-width:340px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;" title="${n(s.url)}">${n(s.url)}</td>
      <td>${n(s.mimeType||"\u2014")}</td>
      <td class="mono">${n(x(s.encodedSizeBytes))}</td>
      <td class="mono">${n(l)}</td>
      <td class="mono">${n(E)}/${n(k)}/${n(I)}</td>
      <td>${g.join(" ")||'<span class="muted">\u2014</span>'}</td>
    </tr>`}).join(`
`),R=o.length>100?`<p class="muted" style="margin:8px 14px 12px;">Showing first 100 of ${String(o.length)} resources.</p>`:"";return`<h2>Resources</h2>
<div class="panel" style="padding:0;overflow:hidden;">
  <p class="muted" style="margin:12px 14px 4px;">${u}</p>
  <table>
    <thead><tr>
      <th>URL</th><th>Type</th><th>Size</th><th>Total ms</th><th>DNS/TCP/TLS</th><th>Tags</th>
    </tr></thead>
    <tbody>
${F}
    </tbody>
  </table>
  ${R}
</div>`}function x(e){return!Number.isFinite(e)||e<=0?"0 B":e<1024?`${String(Math.round(e))} B`:e<1024*1024?`${(e/1024).toFixed(1)} KB`:`${(e/1024/1024).toFixed(2)} MB`}function j(e){let t=e.frames;return!t.root||!t.nodes[t.root]?"":`<h2>Frame tree</h2>
<div class="panel frame-tree">
${M(t.nodes,t.root)}
</div>`}function M(e,t){let r=e[t];if(!r)return"";let a=r.children.length?`<ul>${r.children.map(u=>`<li>${M(e,u)}</li>`).join("")}</ul>`:"",o=[];r.isOOPIF&&o.push("OOPIF"),r.isCrossOrigin&&o.push("cross-origin"),r.isSrcdoc&&o.push("srcdoc"),r.isFenced&&o.push("fenced-frame"),r.detachedAt!==void 0&&o.push("detached");let d=o.length?` <span class="tag warn">${n(o.join(" \xB7 "))}</span>`:"",c=r.inFrameMetrics?.available===!1?r.inFrameMetrics.reason:"",i=c?` <span class="tag warn">opaque: ${n(c)}</span>`:"";return`<div>
  <div><strong>${n(r.frameId)}</strong>${d}${i}</div>
  <div class="frame-url">${n(r.url||"(empty)")}</div>
  ${a}
</div>`}function L(e){if(e.runs.length===0)return"";let t=_(e);if(t.length===0)return"";let r=`<tr><th>Run</th><th>Cold</th>${t.map(o=>`<th class="mono">${n(o)}</th>`).join("")}</tr>`,a=e.runs.map(o=>{let d=t.map(c=>{let i=o.metrics[c];if(!i||!Number.isFinite(i.value))return'<td class="muted">\u2014</td>';let u=c==="cls"?3:1;return`<td class="mono">${n(i.value.toFixed(u))}</td>`}).join("");return`<tr><td>${n(String(o.runIndex))}</td><td>${o.cold?'<span class="tag warn">cold</span>':""}</td>${d}</tr>`}).join(`
`);return`<h2>Per-run values</h2>
<div class="panel" style="padding:0;overflow:hidden;">
  <table>
    <thead>${r}</thead>
    <tbody>${a}</tbody>
  </table>
</div>`}function _(e){let t=new Set;for(let r of e.runs)for(let a of Object.keys(r.metrics))t.add(a);return[...t].sort()}function D(e){let t=Object.keys(e);return t.length===0?"":`<h2>Plugin data</h2>
<div class="panel">
${t.map(a=>{let o=JSON.stringify(e[a],null,2);return`<details>
  <summary>${n(a)}</summary>
  <pre class="code">${n(o)}</pre>
</details>`}).join(`
`)}
</div>`}function J(e){let t=JSON.stringify(e,null,2);return`<h2>Raw report</h2>
<details>
  <summary>Show raw JSON</summary>
  <pre class="code">${n(t)}</pre>
</details>`}function U(e){return`<script type="application/json" id="ohmyperf-report-payload">${h(e)}<\/script>
<script>
  (function () {
    try {
      var el = document.getElementById("ohmyperf-report-payload");
      if (el && el.textContent) {
        window.__OHMYPERF_REPORT__ = JSON.parse(el.textContent);
      }
    } catch (e) {
      console.warn("ohmyperf: could not parse embedded report payload", e);
    }
  })();
<\/script>`}function V(e){try{let t=new URL(e);return`${t.host}${t.pathname==="/"?"":t.pathname}`}catch{return e}}var y=document.getElementById("card"),m=document.getElementById("dropzone"),w=document.getElementById("file-input"),p=document.getElementById("error");function f(e){p&&(p.textContent=e,p.hidden=!1)}function q(){p&&(p.textContent="",p.hidden=!0)}function S(e){q();let t;try{t=JSON.parse(e)}catch(a){f(`Invalid JSON: ${a instanceof Error?a.message:String(a)}`);return}if(!W(t)){f("This file isn't a v1.0.0 OhMyPerf report.");return}let r=$(t,{title:`OhMyPerf \u2014 ${t.meta.url}`});document.open(),document.write(r),document.close()}function W(e){if(!e||typeof e!="object")return!1;let t=e;return!(t.schemaVersion!=="1.0.0"||!Array.isArray(t.runs)||!t.meta||typeof t.meta!="object")}w&&w.addEventListener("change",()=>{let e=w.files?.[0];if(!e)return;let t=new FileReader;t.onerror=()=>f("Could not read file."),t.onload=()=>S(String(t.result??"")),t.readAsText(e)});if(m&&y){for(let e of["dragenter","dragover"])m.addEventListener(e,t=>{t.preventDefault(),t.stopPropagation(),y.classList.add("dragging")});for(let e of["dragleave","drop"])m.addEventListener(e,t=>{t.preventDefault(),t.stopPropagation(),y.classList.remove("dragging")});m.addEventListener("drop",e=>{let t=e.dataTransfer?.files?.[0];if(!t)return;let r=new FileReader;r.onerror=()=>f("Could not read file."),r.onload=()=>S(String(r.result??"")),r.readAsText(t)}),document.addEventListener("paste",e=>{let t=e.clipboardData?.getData("text");t&&t.trim().startsWith("{")&&S(t)})}
